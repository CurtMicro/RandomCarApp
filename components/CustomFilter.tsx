import React from 'react'
import { CustomFilterProps } from '@/types'

const CustomFilter = ({ title: string }: CustomFilterProps) => {
    return (
        <div>CustomFilter</div>
    )
}

export default CustomFilter